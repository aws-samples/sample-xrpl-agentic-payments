# Lambda Layer: xrpl-py and shared dependencies
# Shared across all 8 XRPL Agentic Payments tool Lambda functions

# XRPL SDK (blockchain interaction)
xrpl-py==5.0.0

# Async compatibility (xrpl-py uses asyncio internally)
nest-asyncio>=1.6.0

# Transitive dependency pins (security fixes)
# idna: CVE-2026-45409 (quadratic-time DoS) fixed in 3.15+
idna>=3.15
# httpx: no Lambda in functions/ imports it (it was here for an
# OpenSanctions HTTP call that no longer exists — screening reads the OFAC CSV
# from S3). xrpl-py depends on it, so it lands in the layer regardless and this
# line only enforces the >=0.27.0 floor.
httpx>=0.27.0
